<!DOCTYPE html>
<html>
  <head>
    {% include '/includes/head.html.twig' %}
    <title>Pokedex</title>
  </head>
  <body>
    <div class="jumbotron">
      <img class="title" src="/images/pokemonTitle.png"/>
    </div>
    <div class="container">


    <!-- <table class="table">
        {% if types is not empty %}
            <tr>
                <td>{{ type.getName }}</td>
                <td>{{ type.getStrength }}</td>
                <td>{{ type.getWeakness }}</td>
                {% for pokemon in type.getPokemon %}
                    <td>{{ pokemon.getName }}</td>
                    <td><img src='{{ pokemon.getImagePath }}' class="pokemon"/></td>

                {% endfor %}
            </tr>
        {% endif %}
    </table> -->

            <h1 class="type-item">{{ type.getName }}</a></h1>
            <li class="type-item"><span class="bold">Strong Against:</span> {{ type.getStrength }}</li>
            <li class="type-item"><span class="bold">Weak Against:</span> {{ type.getWeakness }}</li>
            {% for pokemon in type.getPokemon %}
                <h4>{{ pokemon.getName }}</h4>
                <img src='{{ pokemon.getImagePath }}' class="pokemon"/>

            {% endfor %}
        </ul>
    </div>
  </body>
</html>
