<!DOCTYPE html>
<html>
  <head>
    {% include '/includes/head.html.twig' %}
    <title>Pokedex</title>
  </head>
  <body>
    <div class="jumbotron">
      <img class="title" src="/images/pokemonTitle.png"/>
    </div>
    <div class="container">
      <h1>{{ pokemon.getName }}</a></h1>
      <img src='{{ pokemon.getImagePath }}' class="pokemon"/><br><br>
      <li><span class="bold">Dex Number:</span> {{ pokemon.getDexNumber }}</li>
      <li><span class="bold">Size:</span> {{ pokemon.getHeightFeet }}' {{ pokemon.getHeightInches }}"</li>
      <li><span class="bold">Weight:</span> {{ pokemon.getWeight }} pounds</li>

      <form action='/{{ pokemon.getId }}/addPokemon' method='post'>
        <button type='submit' name="add_pokemon" class='btn btn-xs'><img src="/images/pokeball.jpg" class="pokeball"/>Catch</button>
      </form>
    </div>
  </body>
</html>
