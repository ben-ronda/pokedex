<!DOCTYPE html>
<html>
  <head>
    {% include '/includes/head.html.twig' %}
    <title>Pokedex</title>
  </head>
  <body>
      <!--Navbar-->
      {% include '/includes/navbar.html.twig' %}
      <!-- End Navbar -->
    <div class="container">
      <h1 class="pokename">{{ pokemon.getName }}</h1>
      <div class="row">
        <div class="col col-md-6">
          <img src='{{ pokemon.getImagePath }}' class="pokemon"/><br><br>
        </div>
        <div class="col col-md-6">
          <li><span class="bold">Dex Number:</span> {{ pokemon.getDexNumber }}</li><br>
          <li><span class="bold">Size:</span> {{ pokemon.getHeightFeet }}' {{ pokemon.getHeightInches }}"</li><br>
          <li><span class="bold">Weight:</span> {{ pokemon.getWeight }} pounds</li><br>
          {% for type in pokemon.getTypes %}
          <li><span class="bold">Type:</span> <a href="/type/{{ type.getID }}">{{ type.getName }}</a></li><br>
          {% endfor %}
          <li><span class="bold">Description:</span> {{ pokemon.getDescription }}</li>
        </div>
      </div>
    </div>
  </body>
</html>
